<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <div class="container-fluid">
    <%= link_to 'Rich.er', root_with_locale_path, class: 'navbar-brand' %>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">
        <li class="nav-item">
          <%= link_to t('.home'), root_with_locale_path, class: "nav-link #{active_for(path: root_path)}" %>
        </li>
        <% if user_signed_in? %>
          <li class="nav-item">
            <%= link_to t('.transactions'), transactions_path, class: "nav-link #{active_for(controller: 'transactions')}" %>
          </li>
          <li class="nav-item">
            <%= button_to t('.logout'), destroy_user_session_path, method: :delete, class: 'nav-link' %>
          </li>
        <% else %>
          <li class="nav-item">
            <%= link_to t('.login'), new_user_session_path, class: "nav-link #{active_for(path: new_user_session_path)}" %>
          </li>
          <li class="nav-item">
            <%= link_to t('.register'), new_user_registration_path, class: "nav-link #{active_for(path: new_user_registration_path)}" %>
          </li>
        <% end %>
        <li class='nav-item'>
          <% if I18n.locale == :sk %>
            <%= link_to 'EN', { locale: :en }, class: 'text-decoration-none' %>
          <% else %>
            <%= link_to 'SK', { locale: :sk }, class: 'text-decoration-none' %>
          <% end %>
        </li>
      </ul>
    </div>
  </div>
</nav>